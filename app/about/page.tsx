import { Metadata } from "next";

export const metadata: Metadata = {
     title: 'Page About',
     description: 'This profile contains information about us',
};

const PageAbout = () => {
    return <div>PageAbout</div>
};

export default PageAbout;
